using namespace std;
#include<bits/stdc++.h>

map <float,string> mp;
//sscanf (sentence,"%s %*s %d",str,&i);
int main(){
    int n,m;
    char  str[1000];
    //while(scanf("%d %d",&n,&m)!=EOF){
     // for(int i =0; i<n; i++) cin >>
   cin >>  str;
   char ss[1000];
   int ch;
   int mm;
  sscanf(str,"%d %*d",&ch,&mm);
  cout << ch << " " << mm << endl;
return 0;
}
